# 视频自动生成：中文使用说明

这份说明适合把整个 `n8n-video-gemini` 文件夹复制到另一台 Windows 电脑后使用。

## 一、文件夹应该放在哪里

推荐放在：

```text
E:\n8n-video-gemini\
```

启动文件默认使用这个路径。若放到其他盘符或目录，需要同步修改 `.env` 和启动文件中的路径。

## 二、先配置 `.env`

复制 `.env.example` 为 `.env`，然后填写你自己的密钥和接口配置。不要把真实密钥发送给别人或提交到 GitHub。

至少检查以下内容：

```env
GITHUB_TOKEN=你的_GitHub_Token
OPENAI_API_KEY=你的_文本模型_API_Key
OPENAI_CHAT_COMPLETIONS_URL=https://api.aiyxgaw.com/v1/chat/completions
OPENAI_MODEL=gemini-3.5-flash

MOYIN_API_KEY=你的_视频模型_API_Key
MOYIN_API_BASE_URL=https://aihubcc.cc/v1
MOYIN_VIDEO_MODEL=omni-fast
MOYIN_POLL_INTERVAL_SECONDS=8
MOYIN_TIMEOUT_SECONDS=900

N8N_LOCAL_PRODUCTS_DIR=E:/n8n-video-gemini/local_products
N8N_LOCAL_JOBS_DIR=E:/n8n-video-gemini/video_jobs
N8N_LOCAL_OUTPUT_DIR=E:/n8n-video-gemini/video_output
```

保存 `.env` 后，每次重新启动批处理文件，新的配置才会生效。

## 三、放入产品文件夹

每个产品放在下面的目录中：

```text
E:\n8n-video-gemini\local_products\产品文件夹名称\
```

最小示例：

```text
local_products\my_product\
├─ skill_a\
│  └─ product_profile.yaml
└─ images\
   ├─ product_white_bg.png
   ├─ product_detail_side.jpeg
   └─ product_function.png
```

图片也可以放在以下任一目录：

```text
images\
reference_images\
skill_a\images\
skill_a\reference_images\
```

支持 `.jpg`、`.jpeg`、`.png`、`.webp`。建议保留上述三个标准图片名称，方便视频接口稳定读取产品图。

## 四、选择一个启动文件

双击一个与你要使用的脚本风格对应的启动文件：

```text
start_and_run_all_skill_b.bat
start_and_run_all_skill_c.bat
start_and_run_all_skill_e.bat
start_and_run_all_skill_h.bat
start_and_run_all_skill_shelf_single_sample.bat
start_and_run_all_skill_target_shelf_v6.bat
```

启动文件会自动执行：

```text
上传本地产品资料和图片到 GitHub
→ 调用 n8n 生成视频脚本
→ 提交视频生成任务
→ 本地 watcher 轮询任务
→ 下载视频到本地
```

运行期间不要关闭自动打开的 n8n 窗口和 worker watcher 窗口。不要同时运行两个启动文件。

## 五、在哪里找到生成的视频

视频会保存到：

```text
E:\n8n-video-gemini\video_output\产品文件夹名称\任务编号\
```

生成成功后，里面会有 `.mp4` 视频文件。

## 六、出错时看哪里

```text
OpenAI 脚本生成日志：E:\n8n-video-gemini\openai_jobs\任务目录\openai.log
视频任务状态：      E:\n8n-video-gemini\video_jobs\任务编号\status.json
视频下载/轮询日志：  E:\n8n-video-gemini\video_jobs\任务编号\worker.log
```

若窗口只显示“500 内部服务器错误”，请打开 n8n 页面中的 Executions，点击失败的节点查看具体报错；500 只是外层提示，不是根因。

## 七、新电脑首次使用还需要什么

1. 安装 Node.js 和全局 n8n。
2. 安装 Python 3，并确认命令行中 `python --version` 能显示版本号。
3. 复制整个 `E:\n8n-video-gemini` 文件夹。
4. 在新电脑单独创建和填写 `.env`，不要复制别人的真实密钥。
5. 启动 n8n 后，导入并 Publish：

```text
workflows\main_generate_video.workflow.json
workflows\local_product_upload_to_github.workflow.json
```

完成以上步骤后，就可以把产品放进 `local_products`，再双击对应的启动文件。
